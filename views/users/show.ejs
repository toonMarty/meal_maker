<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <title>Meal Maker | Users</title>
    </head>
    <body class="show-user-details">
        <div class="card mx-auto" style="width: 25rem; margin-top: 10vh;">
          <img src="/images/profile-default.jpg" class="card-img-top" alt="person-in-frame">
          <div class="card-body text-center">
            <h5 class="card-title my-3"><%= user.fullName %></h5>
            <p class="card-text text-muted mb-1">
              <img src="/images/email-logo.png" style="float: left;" height="24" width="24">
              <%= user.email %>
            </p>

            <p class="card-text text-muted mb-1">
              <img src="/images/zip-code.png" style="float: left;" height="24" width="24">
              <%= user.zipCode %>
            </p>
            <p class="card-text text-muted mb-4" type="password">
              <img src="/images/password-logo.png" style="float: left;" height="24" width="24">
              <%= user.password %>
            </p>

            <a class="btn btn-success all-users-btn" href="/users">All Users</a>
            <a class="btn btn-primary edit-user-btn" href="<%= `/users/${user._id}/edit` %>">Edit User</a>
            <a class="btn btn-danger delete-user-btn" href="<%= `/users/${user._id}/delete?_method=DELETE` %>" onclick="return confirm('Are you sure you want to delete this user')">Delete User</a>
          </div>
        </div>

        <% if (user.subscribedAccount) { %>
        <h4 class="text-center" style="font-family: Arial, Helvetica, sans-serif; font-weight: bold; color: white;"><%= user.name.first %> has a<br>
            <a href="<%=`/subscribers/${user.subscribedAccount}` %>" class="subscribed">subscribed account</a>
        </h4>
        <% } %>

        
    </body>
</html>